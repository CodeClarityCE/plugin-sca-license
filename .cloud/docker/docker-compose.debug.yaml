services:
  plugin-js-license:
    # For dev we keep the golang container and install devtools
    build:
      target: plugin-debug
    image: ceherzog/js-license-debug:latest
    ports:
      - 40000:40000
