services:
  plugin-license-finder:
    # For dev we keep the golang container and install devtools
    build:
      target: plugin-debug
    image: codeclarityce/license-finder-debug:latest
    ports:
      - 40000:40000
